apiVersion: v1
kind: Service
metadata:
  name: content-api-svc
  namespace: content-platform
  labels:
    app: content-api
spec:
  selector:
    app: content-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
